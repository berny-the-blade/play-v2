{
  "name": "domino-pernambucano-play-v2",
  "version": "1.0.0",
  "private": true,
  "description": "Domino Pernambucano - compiled build (esbuild + Tailwind CLI), replaces runtime Babel/Tailwind CDN transpilation",
  "scripts": {
    "build:js": "esbuild src/app.jsx --bundle --minify --target=es2018 --outfile=bundle.js",
    "build:css": "tailwindcss -i src/input.css -o output.css --minify",
    "build": "npm run build:js && npm run build:css",
    "watch:js": "esbuild src/app.jsx --bundle --target=es2018 --outfile=bundle.js --watch"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "tailwindcss": "^3.4.13"
  }
}
